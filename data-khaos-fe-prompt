请生成BI拖拽看板的ECharts图表组件，严格遵守【Skill‑BI‑Dashboard‑V2】全部约束，本场景是栅格拖拽布局，页面同时存在多个卡片图表，卡片支持拖拽缩放。

【外层卡片容器约束（最高优先级，防止图表互相压盖重叠）】
1. 每个图表完全渲染在自己独立卡片容器内，CSS设置overflow:hidden; position:relative；严禁多个图表DOM互相重叠、交叉渲染，不允许一个图表侵入其他卡片DOM区域。
2. 图表继承父卡片100%宽高，禁止写死width/height像素。
3. tooltip开启confine:true，悬浮提示框锁死在当前卡片内部，禁止飘到其他卡片上。
4. z-index作用域仅限当前卡片，不能穿透覆盖其他卡片。

【ECharts图表强制约束，重点处理仪表盘、环形图】
1. 必须配置grid，预留left/right/top/bottom边距，防止坐标轴文字被裁切。
2. 仪表盘gauge、环形饼图pie，**禁止写死固定半径**，需要根据容器尺寸动态计算半径，卡片缩小时图形自动缩小，保证图形完整落在卡片内部，不溢出。
3. 监听容器resize事件，调用echarts实例resize；组件销毁时销毁echarts实例，避免多实例冲突和内存泄漏。
4. X/Y轴文字过长做截断/旋转；图例过多自动换行；饼图小占比自动合并，标签不溢出。
5. 处理空数据，展示友好空状态，不渲染错乱图形。

【UI视觉：商务简约BI风格】
1. 网格线浅灰色弱化；面积图透明度0.2‑0.3；配色区分度高，支持深浅主题切换。
2. 字体层级：指标大数字突出，标题次之，坐标轴图例字号最小，卡片缩小时自动降级字号。

【禁止清单】
禁止图表DOM互相重叠压盖；禁止写死固定宽高；禁止仪表盘/环形图溢出卡片；禁止tooltip跨卡片乱跑；禁止不做resize监听；禁止内存泄漏。

输出完整Vue3 setup组件，props接收数据，包含动态半径计算逻辑、resize、实例销毁，关键位置写注释。


如果出现问题，请重新检查
卡片缩放后，仪表盘/环形图图形溢出卡片，多个图表发生DOM重叠互相压盖。
请对照Skill‑BI‑Dashboard‑V2约束检查：
1.确认卡片容器 overflow:hidden; position:relative；
2.gauge/pie不要硬编码半径，实现根据容器尺寸动态计算半径；
3.确认echarts实例只渲染在自己的DOM节点，不要越界；
4.确认tooltip confine:true生效。
再次修复完整组件。